<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="JellyfinMauiClient.MainPage"
             Title="Filmes">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Atualizar" Command="{Binding LoadCommand}" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto,*" Padding="12">
        <StackLayout Spacing="6">
            <ActivityIndicator IsVisible="{Binding IsBusy}" IsRunning="{Binding IsBusy}" />
            <Label Text="{Binding ErrorMessage}" TextColor="Red" IsVisible="{Binding ErrorMessage, Converter={StaticResource NullToBoolConverter}}" />
        </StackLayout>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Movies}">
            <CollectionView.EmptyView>
                <Grid Padding="24">
                    <Label Text="Nenhum filme encontrado." HorizontalOptions="Center" VerticalOptions="Center" />
                </Grid>
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8" ColumnDefinitions="*,Auto">
                        <Label Text="{Binding Title}" FontAttributes="Bold" />
                        <Label Grid.Column="1" Text="{Binding ProductionYear}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
